{% extends 'base.html.twig' %}

{% block title %}Espace Artiste{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-12">
                <h1>Espace Artiste</h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Bienvenue {{ user.prenom }} {{ user.nom }}</h5>
                        <p class="card-text">Vous êtes dans votre espace artiste.</p>

                        {% if user.styleArtistique %}
                            <p><strong>Style artistique :</strong> {{ user.styleArtistique }}</p>
                        {% endif %}

                        {% if user.biographie %}
                            <p><strong>Biographie :</strong> {{ user.biographie }}</p>
                        {% endif %}

                        <a href="{{ path('app_profile_edit') }}" class="btn btn-primary">Modifier mon profil</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
