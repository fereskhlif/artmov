{% extends 'base.html.twig' %}

{% block title %}Espace Client{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-12">
                <h1>Espace Client</h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Bienvenue {{ user.prenom }} {{ user.nom }}</h5>
                        <p class="card-text">Vous êtes dans votre espace client.</p>
                        <a href="{{ path('app_profile_edit') }}" class="btn btn-primary">Modifier mon profil</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
